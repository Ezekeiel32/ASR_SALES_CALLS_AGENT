fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.30.0,<0.31.0
httpx>=0.27.0,<0.28.0
typer>=0.12.0,<0.13.0
click>=8.1.7,<9.0.0
pydantic>=2.7.0,<3.0.0
pydantic-settings>=2.4.0,<3.0.0
python-dotenv>=1.0.1,<2.0.0
tenacity>=8.2.0,<9.0.0
anyio>=4.4.0,<5.0.0
python-multipart>=0.0.9,<0.0.10
openai>=1.45.0,<2.0.0

# Database
sqlalchemy>=2.0.23,<3.0.0
alembic>=1.12.1,<2.0.0
psycopg2-binary>=2.9.9,<3.0.0
pgvector>=0.2.4,<0.3.0

# Storage & Queue
boto3>=1.34.0,<2.0.0
celery>=5.3.4,<6.0.0
redis>=5.0.1,<6.0.0
runpod>=1.0.0  # For RunPod Serverless endpoints

# Audio Processing - NOT NEEDED in API server
# Audio processing (transcription, diarization, voiceprint) happens on RunPod Serverless GPU workers
# librosa, soundfile, pydub removed - only needed in RunPod workers (requirements.txt)

# ML/AI - NOT NEEDED in API server  
# PyTorch and CUDA libraries removed - only needed in RunPod workers (requirements.txt)
# The API server only handles HTTP requests and database operations
# Heavy ML processing (diarization, voiceprint generation) runs on RunPod Serverless
numpy>=1.24.0,<2.0.0  # Still needed for some data processing (XGBoost, pandas)

# NLP
spacy>=3.7.0,<4.0.0

# XG Agent (LangGraph/XGBoost)
langgraph>=0.2.0,<1.0.0
langchain-nvidia-ai-endpoints>=0.1.0,<1.0.0
langchain-core>=0.3.0,<1.0.0
xgboost>=2.0.0,<3.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
plotly>=5.18.0,<6.0.0
google-api-python-client>=2.0.0,<3.0.0
google-auth-httplib2>=0.2.0,<1.0.0
google-auth-oauthlib>=1.2.0,<2.0.0

# Security
python-jose[cryptography]>=3.3.0,<4.0.0
passlib[bcrypt]>=1.7.4,<2.0.0
bcrypt>=4.0.0,<5.0.0  # Pin bcrypt version for passlib compatibility