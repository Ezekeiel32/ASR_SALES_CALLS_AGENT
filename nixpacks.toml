[phases.setup]
nixPkgs = ["python312", "postgresql_16", "redis"]

[phases.install]
cmds = [
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]

[start]
cmd = "uvicorn agent_service.api:app --host 0.0.0.0 --port $PORT"

